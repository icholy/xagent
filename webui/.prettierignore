node_modules
dist
dist-ssr
build
coverage
pnpm-lock.yaml
src/gen
# Auto-generated by the TanStack Router vite plugin on dev/build. Its formatting
# (printWidth 80) differs from this repo's prettier config (printWidth 100), so
# leaving it under format:check caused perpetual churn — every dev/build rewrote
# it. Ignore it and commit the generator's native output.
src/routeTree.gen.ts
